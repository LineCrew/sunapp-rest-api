import { DataTypes } from 'sequelize';
import sequelize from '../../common/dbConfig';

/**
 * 광고 엔티티
 */

const AdvertiseEntity = sequelize.define('playingHistory', {
});

export default AdvertiseEntity;
